//Declare objects from our class.

/*Mover m;
Mover obj2;
//Define Variables from the class.
int xSpeed1=0;
  int xSpeed2=0;
  float acc1=0;
  float acc2=0;
 

//Setup function, declare the size and define the parameters for the objects m and obj2 respectively.

void setup(){
size(1000,1000);

 
  m = new Mover(125,125,xSpeed1,50,0,acc1);
  obj2 = new Mover(125,125,xSpeed2,50,0,acc2);


}

//Draw function.

void draw(){
  
//set background color to white

background (255);

//creation of cannon base, including the giant rectangle, the small triangle and the "barrel" of the cannon that rests on it. Also creates the two rectangles
//in the top right and bottom corners of the screen.

rect(0,200,200,1000);
triangle(100,150,75,200,125,200);
rect(100,100,100,50);
rect(950,0,50,50);
rect(950,950,50,50);

//Display and update the two objects respectively.

m.display();
m.update();
obj2.display();
obj2.update();

//For debugging purposes, print out the speed

print(xSpeed1);
}
*/
